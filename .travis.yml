language: python
python:
  - "2.7"
  - "3.6"
  - "3.7"

jobs:
  beofre:
  - python: "3"
    before_install:
      - pip install salib

install: 
  - pip install numpy cython 
  - pip install smt
  - pip install -e .
  - pip list

script:
  - pytest